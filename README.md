# 🎨 Random Pic Plugin for AstrBot

<<<<<<< HEAD
一个用于 [AstrBot](https://github.com/AstrBotDevs/AstrBot) 的随机图片插件，支持从指定 API 获取随机图片，提供单图和多图获取功能，并支持 R18 模式、白名单管理和群组配置。

---

## 📦 安装

1. 在插件文件夹下执行
```
git clone https://github.com/YUGANJUN114514/astrbot_plugin_random_pic
```
2. 重启 AstrBot

---

## ⚙️ 配置说明

在 AstrBot 管理面板或 `_conf_schema.json` 中配置以下参数：

| 配置项 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| `wh_list` | list | `[]` | 多图图用户白名单，留空则全部用户可用 |
| `many_gr_list` | list | `[]` | 多图图群组白名单，留空则全部群组可用 |
| `max_num` | int | `5` | 多图图最大获取数量，建议不要设置过大 |
| `R18` | bool | `false` | 全局 R18 开关，关闭时无论其他设置都不会发送 R18 图片 |
| `R18_gr_mode` | list | `[]` | R18 群组白名单，为空则按全局 R18 设置处理 |

---

## 🚀 使用方法

### 单图命令

```
图图 [r18:on|off]
```
获取一张随机图片。别名：`tutu`、`pic`

### 多图命令

```
多图图 <数量> [r18:on|off]
```
获取多张随机图片。别名：`duotutu`、`manypic`

**示例：**
- `多图图 5` — 获取 5 张普通图片
- `多图图 3 r18:on` — 获取 3 张 R18 图片（需权限）
- `多图图 10 r18:off` — 获取 10 张普通图片

> ⚠️ 多图图命令目前仅支持 `aiocqhttp` 平台（QQ 协议端）。

---

## 🔧 管理员命令

以下命令仅限管理员使用：

### 用户白名单管理
```
图图配置 用户白名单 list              # 查看白名单
图图配置 用户白名单 add <用户ID>      # 添加用户
图图配置 用户白名单 remove <用户ID>   # 移除用户
图图配置 用户白名单 help              # 查看帮助
```

### 群组白名单管理
```
图图配置 群组白名单 list              # 查看白名单
图图配置 群组白名单 add <群组ID>      # 添加群组
图图配置 群组白名单 remove <群组ID>   # 移除群组
图图配置 群组白名单 help              # 查看帮助
```

### R18 群组管理
```
图图配置 群组r18 list                 # 查看 R18 群组列表
图图配置 群组r18 add <群组ID>         # 添加群组到 R18 白名单
图图配置 群组r18 remove <群组ID>      # 从 R18 白名单移除群组
图图配置 群组r18 help                 # 查看帮助
```

### 全局 R18 开关
```
图图配置 r18 on     # 开启全局 R18
图图配置 r18 off    # 关闭全局 R18
图图配置 r18 list   # 查看当前状态
图图配置 r18 help   # 查看帮助
```

### 最大数量设置
```
图图配置 最大数量          # 查看当前最大数量
图图配置 最大数量 <数字>   # 设置最大数量
```

---

## 🔒 R18 模式优先级

R18 模式的判断优先级如下：

1. **全局 R18 开关** (`R18`) — 如果关闭，强制关闭 R18
2. **用户临时参数** (`cr18`) — 多图图命令中的 `r18:on/off` 参数
3. **群组 R18 白名单** (`R18_gr_mode`) — 当前群组是否在白名单中
4. **全局默认** — 未设置时按全局 `R18` 值处理

---

## 📝 更新日志

### v1.0.0
- ✅ 单图获取功能
- ✅ 多图获取功能（支持合并转发消息）
- ✅ R18 模式支持（全局 + 群组）
- ✅ 用户白名单管理
- ✅ 群组白名单管理
- ✅ 图片去重（基于 URL）
- ✅ 自动重试机制（最多 3 次）

---

## 🤝 其他

- 本项目仅供学习交流使用，请遵守当地法律法规。
- 使用了[ManyAcg API](https://manyacg.top/about)
- 代码很烂,而且这是我第一次写项目QwQ
- 欢迎各位大佬提出建议和意见
- 如果你觉得这个项目对你有帮助，请给我点个star吧！（应该不会有）

---